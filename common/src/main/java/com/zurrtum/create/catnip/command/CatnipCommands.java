package com.zurrtum.create.catnip.command;

import com.mojang.brigadier.CommandDispatcher;
import com.mojang.brigadier.tree.LiteralCommandNode;
import net.minecraft.commands.CommandSourceStack;

public class CatnipCommands {
    public static void createOrAddToShortcut(CommandDispatcher<CommandSourceStack> dispatcher, String shortcut, LiteralCommandNode<CommandSourceStack> target) {
    }
}
