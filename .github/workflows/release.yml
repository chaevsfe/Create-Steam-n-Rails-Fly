name: Build Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Loom Cache
        uses: actions/cache@v4
        with:
          path: "**/.gradle/loom-cache"
          key: "${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}"
          restore-keys: "${{ runner.os }}-gradle-"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-home-cache-cleanup: true
          cache-read-only: true

      - name: Fetch the Create Fly Recipe Viewer jar nested into the build
        env:
          VIEWER_JAR_NAME: CreateReiViewer-1.0.0-fly.26.1.2-beta.2+fabric-mc26.1.2.jar
          VIEWER_JAR_URL: https://github.com/chaevsfe/CreateReiViewer-Fly/releases/download/1.0.0-fly.26.1.2-beta.2/CreateReiViewer-1.0.0-fly.26.1.2-beta.2%2Bfabric-mc26.1.2.jar
        run: |
          mkdir -p libs
          curl -fsSL -o "libs/$VIEWER_JAR_NAME" "$VIEWER_JAR_URL"

      - name: Build
        run: ./gradlew clean build validateAccessWidener --no-build-cache --no-daemon

      - name: Upload distributable JAR
        uses: actions/upload-artifact@v4
        with:
          name: steam-rails-fabric-26.1.2-release
          path: |
            build/libs/Steam_Rails-*.jar
          if-no-files-found: error
